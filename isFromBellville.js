function isFromBellville(registration){
    if(registration.startsWith('CY')){
      return true
    }else {
      return false
    }
  }
  function isFromBellville(registration){
    if(registration.startsWith('CA')){
      return false
    }else {
      return true
    }
  }